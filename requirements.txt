requests>=2.31
pytest>=7.4
psutil>=5.9
pyinstaller>=6.0  # only needed to build the standalone .exe, not to run `py main.py`
fastapi>=0.110
uvicorn>=0.27
httpx>=0.27  # test-only - FastAPI's own TestClient needs it, not used at runtime
